<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Grocery.App.ViewModels"
             x:Class="Grocery.App.Views.CategoriesView"
             Title="CategoriesView">
    <Shell.TitleView>
        <Grid>
            <Label Text="Productcategorieën" FontSize="Large" HorizontalOptions="Center" VerticalOptions="Center"/>
        </Grid>
    </Shell.TitleView>
    <Border Stroke="#C49B33" StrokeThickness="4" Padding="10" Margin="10">
        <CollectionView x:Name="categoriesCollectionView"
                        ItemsSource="{Binding Categories}" Margin="20"
                        SelectionMode="Single"
                        SelectionChangedCommand="{Binding SelectCategoryCommand}"
                        SelectionChangedCommandParameter="{Binding SelectedItem, Source={RelativeSource Self}}">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="10"/>
            </CollectionView.ItemsLayout>
            <CollectionView.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Text="#" Grid.Column="0" FontAttributes="Bold" HorizontalTextAlignment="Start" FontSize="Subtitle"/>
                    <Label Text="Categorie" Grid.Column="1" HorizontalTextAlignment="Start" FontAttributes="Bold" FontSize="Subtitle"/>
                </Grid>
            </CollectionView.Header>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label Text="{Binding Id}" Grid.Column="0" HorizontalOptions="Start"/>
                        <Label Text="{Binding Name}" Grid.Column="1" HorizontalOptions="Start"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Border>
</ContentPage>